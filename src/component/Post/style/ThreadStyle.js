const style = (theme) => ({
    paper: {
        maxWidth: 'vw',
        margin: `${theme.spacing(1)}px auto`,
        padding: theme.spacing(2),
        borderWidth: 10,
        borderColor: 'black'
    },
    textfield: {
        width: '100%',
    },
});

export default style;